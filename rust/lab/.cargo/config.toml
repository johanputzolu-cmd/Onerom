[build]
target = "thumbv7em-none-eabi"

[target.thumbv7em-none-eabi]
runner = "probe-rs run --no-location --chip STM32F405RGTx"

[profile.dev]
opt-level = "s"

[profile.release]
opt-level = 3
debug = 2
lto = "fat"
codegen-units = 1
panic = "abort"

[env]
DEFMT_LOG = "info"
